[package]
name = "ray-agent"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
rand = "0.8"
axum = "0.7"
serde = { version = "1.0", features = ["derive"] }